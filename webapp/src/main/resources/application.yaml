server:
  port: 80
  error:
    include-message: always
    include-stacktrace: on_param
spring:
  datasource:
    url: jdbc:h2:./db/genealogy;AUTO_SERVER=TRUE
    username: xxx
    password: xxx
  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 10
          order_inserts: true
          order_updates: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  jackson:
    default-property-inclusion: non_null
security:
  secret: kdskliojfsdl32r3hhkjdfdf
  token:
    exp: 10 #minutes

#    schema: classpath:sql/schema.sql
#    initialization-mode: always

---
spring:
  config:
    activate:
      on-profile: prod
server:
  port: 8081

---
spring:
  config:
    activate:
      on-profile: int-test
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
  jpa:
    show-sql: true
    generate-ddl: true

---
spring:
  config:
    activate:
      on-profile: pg-local
  datasource:
    url: jdbc:postgresql://localhost:5432/genealogy
    username: pgdba
    password: coldir
  jpa:
    show-sql: true
    generate-ddl: true
